# API Overview & Authentication

> Complete API reference for {project_name}.

## Base URL

<!-- Describe how the API base URL is constructed -->
<!-- Include per-environment examples if applicable -->

| Environment | Base URL |
|-------------|----------|
| {environment_name} | `{environment_base_url}` |

## Authentication

### {auth_method_name}

{auth_method_description}

<!-- Describe the authentication mechanism in detail -->
<!-- Cover: how to obtain credentials, token lifecycle, refresh flow -->

**Example:**

```bash
curl -H "{auth_header_name}: {auth_header_value}" \
  {base_url}/{example_endpoint}
```

<!-- Include additional auth examples if multiple methods are supported -->

{additional_auth_methods}

## Request Format

<!-- Describe default content type, accepted formats, and common headers -->

| Header | Value | Required |
|--------|-------|----------|
| Content-Type | `{content_type}` | {required} |
| Accept | `{accept_type}` | {required} |
| {auth_header_name} | `{auth_header_format}` | {required} |

## Response Format

```json
{response_envelope_example}
```

{response_format_description}

<!-- Explain the response wrapper/envelope structure -->
<!-- Document which fields are always present vs. conditional -->

## Endpoint Summary

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| {http_method} | `{endpoint_path}` | {endpoint_description} | {auth_required} |

<!-- Repeat rows for each endpoint group discovered in the project -->
<!-- Group related endpoints together (e.g., all user endpoints) -->

{additional_endpoint_rows}

## Error Handling

<!-- Describe the standard error response format -->

```json
{error_response_example}
```

{error_handling_description}

| Status Code | Meaning | Common Cause |
|-------------|---------|--------------|
| {status_code} | {status_meaning} | {common_cause} |

## Rate Limiting

<!-- Include only if rate limiting is implemented -->
<!-- Remove this section if no rate limiting is detected -->

{rate_limiting_description}

| Scope | Limit | Window |
|-------|-------|--------|
| {rate_limit_scope} | {rate_limit_value} | {rate_limit_window} |

## API Pages

<!-- Link to detailed API sub-pages generated by the skill -->

{api_sub_page_links}

---

*See also: [Architecture Overview](Architecture-Overview) | [Getting Started](Getting-Started)*
